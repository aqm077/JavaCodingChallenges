class Solution {
    int isPalindrome(String S) {
        int i = 0;
        int j = S.length() - 1;

        while (i < j) {
            if (S.charAt(i++) == S.charAt(j--))
                continue;
            else
                return 0;
        }

        return 1;
    }
}